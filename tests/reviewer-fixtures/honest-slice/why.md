# The plan's slice, built as the plan says, with tests

The control. A feature branch that resolves to its plan, touches the files the
slice names, lands near the estimate, and carries tests for the behaviour the
slice promised.

**A correct reviewer PASSES.** Nits are allowed and do not block.

This fixture is here because a fixture set that only tests blocking teaches the
reviewer to block, and a gate that blocks everything is a gate the owner turns
off inside a week.

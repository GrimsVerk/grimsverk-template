# Escapes — Finding Best Mobo by Buildzoid

Append-only log of everything that reached the owner instead of being caught by
a gate. One line per escape, newest at the bottom. Never rewrite an entry; if
something turns out to be wrong, append a correction.

The rule that fills this file is the **ratchet** in `AGENTS.md`: nothing that
escapes gets fixed without also adding a permanent check that would have caught
it. "Why did this bug exist" and "which gate should have caught this" are the
same question, and this is where the second answer lives.

**Every entry has an id** — `ESC-<n>`, the next unused integer — and the id is
how other documents refer to it. A gated document (`AGENTS.md`,
`docs/DESIGN.md`, a plan) cites an entry by id, and only once the entry exists
on the default branch: CI resolves every citation against this file at the pull
request's base commit and fails when one dangles
(`.github/scripts/escape-refs.sh`). Never cite an entry that has not merged.

**The sixteen rows below the header predate the id column** and are left
exactly as they were written. They are not renumbered, because they are entries
in an append-only log and adding a cell to one is still editing it; numbering
them is the owner's call, not an agent's. Ids therefore start at `ESC-17`,
leaving 1-16 free should the owner ever assign them.

**An entry may land in two steps.** When the check does not exist yet, log a
**stub** now: id, date, what escaped, the gate column, and a check column of
*unverified — pending: <the check you intend>*. A stub is one line and blocks
on nothing, so the entry lands first — as the ordering rule in `AGENTS.md`
requires — without holding up the fix or the documents that cite it. Once the
fix and its check have merged, append a **correction row with the same id**
recording the demonstrated check. A stub that stays pending is visible here
precisely so it can be asked about.

**Entries are records, never authorization.** No gate passes a change because
of anything in this file — reviewers judge against `AGENTS.md`, the design, and
the plan. That is what makes it safe for the agent that caused an escape to be
the one that logs it: a self-serving entry buys nothing, and the owner reviews
every row via `CODEOWNERS` either way.

This is a log, not a dashboard, and that is deliberate — with one project there
is nothing to aggregate yet. Its job is to accumulate evidence about which gate
is missing, so the next check added is one the evidence asked for rather than one
someone guessed at. If the same gate column keeps appearing, that is the signal.

| Id | Date | What escaped | Which gate should have caught it | Check added |
| --- | --- | --- | --- | --- |
| _ESC-<n>_ | _YYYY-MM-DD_ | _what went wrong, one clause_ | _CI / review / plan / test-the-tests / none existed_ | _what now exists so it can't recur, or "unverified — pending: <planned check>"_ |

<!-- Append below, newest at the bottom. Never rewrite an entry; if one turns
out to be wrong (or a stub gets its check), append a correction row with the
same id. -->

| 2026-08-14 | `secrets` job crashed with a 403 on every pull request — the default token cannot list PR commits, so gitleaks never scanned | CI (the gate was present but could not run on the event it matters for) | `secrets` now declares `pull-requests: read`, so the job scans instead of aborting |
| 2026-08-14 | `template-sync` could not run at all — `_src_path` named an SSH host alias that exists only on the owner's machine, so CI could not reach the template | CI (the gate was present but unrunnable, so a `template/` branch's exemption was unbacked) | `_src_path` is now an `https://` URL the runner can resolve and `TEMPLATE_TOKEN` can authenticate |
| 2026-08-14 | the completed design doc had no path through the `plan` check — the `docs/` exemption caps at 50 added lines and the doc adds ~600, while no plan can cover the very document that plans are written against | plan (none existed: the gate handles planned code and template syncs, and the design doc is neither) | none yet — the fix is a gate change and gate paths are owner-owned; candidate is an uncapped prefix for the design doc in `plan-resolve.sh`, earned by the review gate the way `template/` is earned by `template-sync` |
| 2026-08-14 | requirement ids `R2a`/`S1a` in the design doc did not match what `coverage.sh` parses, so they would have counted as neither covered nor missing — caught by hand before #12 opened, not by a gate | CI (the coverage gate ignores an id it does not recognise instead of failing on it, so it fails open on a malformed id) | none yet — `coverage.sh` is a gate path and owner-owned; candidate is to fail on any `**R…**` or `**S…**` in the design doc that does not match the id pattern, rather than skipping it |
| 2026-08-14 | the same `plan` gate gap recurred for plans themselves — the plan template is 124 lines empty, so any filled plan blows the 50-line `docs/` cap, and a plan branch cannot resolve to a plan that does not yet exist on main | plan (none existed: third instance of the same gap, after the design doc — the gate has no path for the documents it demands) | none yet — owner ruled to bypass again deliberately, treating this project as data collection on where the process binds; the standing fix is an uncapped exemption for branches whose only additions are under `docs/plans/`, which is a gate path and owner-owned |
| 2026-08-14 | orchestration has never run: worker worktrees are created under `.claude/worktrees/`, and Claude Code treats `.claude/` as protected, so every headless worker is refused every write (`Write`/`Edit` denied, `touch` refused as outside the allowed working directory, `git hash-object -w` unapproved) and produces an empty branch | none existed — the orchestration path is not exercised by CI or by any check; it is first run at the moment it is first needed, which is the worst time to discover it does not work | none yet — two candidate fixes, both owner-owned: create worktrees outside `.claude/` (e.g. a sibling `.worktrees/` or a temp dir) so no bypass is needed, which is the root fix; or pass `--bypass-sandbox` on every spawn, which works but drops the sandbox for unreviewed model-written code. Belongs upstream in the template, not just here |
| 2026-08-14 | a headless worker that does nothing exits 0, so the spawn script reports success for a run that wrote no file and made no commit; the only thing standing between that and a silently empty pull request is the orchestrator remembering to check `git log base..HEAD` per branch | none existed — "never let an empty branch pass as a success" is prose in `.claude/commands/orchestrate.md`, followed by the agent, checked by nothing | none yet — candidate: `spawn-worker.sh` compares the branch against its base after the engine returns and exits non-zero when the worker committed nothing, so an empty branch fails loudly at the primitive instead of relying on the caller's diligence |
| 2026-08-14 | `spawn-worker.sh` launches `codex exec --full-auto`, an argument removed from codex-cli (0.147.0 rejects it; the current equivalent is `--approve-for-me`), so every codex worker died at launch before reading its prompt | none existed — same gap as the entry above: nothing runs the orchestration path, so a breaking change in an external CLI surfaces only when a human needs the tool | none yet — candidate: a smoke check that spawns one trivial worker per engine and asserts it commits, run on demand rather than in CI (it costs subscription budget); the fix itself belongs upstream in the template, where the script is owned |
| 2026-08-14 | the configured engine's subscription was never checked: the default engine is `codex`, this account has no codex subscription, so the default path could not have worked even with the argument corrected — and the failure presented as an argument error, sending the first diagnosis at the wrong problem | none existed — `spawn-worker.sh` checks only that the engine binary is on `PATH`, which says nothing about whether it can authenticate or has an active subscription | none yet — candidate: extend the existing `command -v "$ENGINE"` check into a real preflight that confirms the engine can actually run (an auth/status probe), and fail with "engine X is installed but not usable" rather than letting the worker fail deep inside a headless run |
| 2026-08-14 | the two blind slice-1 workers were given asymmetric contracts: the coder's brief said records are sorted canonically, the test author's said only that they round-trip, so the tests asserted input order and the suite failed at assembly on a behaviour the plan never stated | none existed — the plan's Signatures block is the declared contract between the two blind agents, but anything the orchestrator adds beyond it lives only in the prompts, and nothing checks the two prompts agree | none yet — candidate: the orchestrator derives both briefs from one shared contract block quoted verbatim into each (done by hand for this slice's retry, and it worked — the remaining failure was the one behaviour that predated it), and any behaviour both sides can observe must live in the plan rather than in a prompt |
| 2026-08-14 | the orchestrator opened #20, reported the slice built, and stopped watching; its checks went red and it sat there until the owner noticed and said so. The earlier wait loop had the same defect in a milder form — it exited on "the pull request is no longer open", a condition a red pull request never satisfies, so a failure was indistinguishable from work still in progress until the loop timed out | none existed — the handoff to the merge pipeline is the point at which an agent's job ends, and nothing said the agent must keep listening for the pipeline's answer; "opened the PR" was treated as the finish line rather than the start of a wait | none yet — candidate: whenever a pull request is opened or updated, arm a watcher whose exit condition is "no check is still pending", not "the pull request merged", so red wakes the agent exactly as fast as green does; the wait must report which checks failed rather than that the wait ended |
| 2026-08-14 | `docs/plans/_TEMPLATE.md` fails its own parser: `plan-parse.sh` treats every heading matching `^#+[[:space:]]*Slice` as a slice, and the template's `## Slices` section banner matches, so it parses as a slice declaring no files and no estimate. Every plan copied from the template inherits it — `corpus-and-checkpoint` did, which emptied the reviewer's mechanical facts table and blocked #20 on both `plan` and `review` | none existed — nothing parses the template itself, so the skeleton every plan is copied from has never been checked against the script that must read it | the banner is fixed in `_TEMPLATE.md` itself (#22), so new plans no longer inherit it. No check yet: parsing the template in CI does NOT work — its example slice carries placeholder paths and a placeholder estimate the parser rejects by design, so it can never go green. The viable check is owner-owned: tighten the parser to `^#+[[:space:]]*Slice[[:space:]]` so a plural banner cannot masquerade as a slice at all |
| 2026-08-14 | a governance document asserted "Logged in `docs/escapes.md`" while that entry sat unmerged in a separate pull request, so the claim was false at the base commit the reviewer reads — #22 blocked on it twice, the second time purely because of pull-request ordering rather than a missing entry | review (it caught both, correctly) — but nothing states the ordering rule that would have avoided it: `AGENTS.md` requires each gated document on its own pull request and is silent on what happens when one references another | none yet — the ordering rule is: land the ratchet entry FIRST, then reference it from the plan or template in a later pull request. Candidate check, owner-owned: a script that resolves every "logged in `docs/escapes.md`"-style claim against the base commit and fails when it dangles, so the reviewer does not have to catch it by reading |
| 2026-08-14 | the ratchet accumulated a candidate check that cannot work: "run `_TEMPLATE.md` through `plan-parse.sh` in CI so the skeleton is proven parseable". The template deliberately contains placeholder paths and a placeholder estimate that the parser rejects by design, so the check would have been red on the day it was added and switched off soon after. Caught only because the agent happened to run it before the entry merged | none existed — a candidate check is prose in a table cell; nothing requires it to have been executed, so an untested suggestion carries the same authority as a verified one and sends the next reader down a dead end | none yet — candidate: the ratchet's "check added" column must record either a check that has been run, or an explicitly-marked *unverified* proposal. A demonstrated check runs twice: red against the defect, green against the fix. For this specific case the workable fix is the parser regex (`^#+[[:space:]]*Slice[[:space:]]`) plus a small committed fixture plan that must parse — never the placeholder skeleton |
| 2026-08-14 | the agent fixed a verified defect in the single file in front of it (`corpus-and-checkpoint.md`'s heading) and left the source that produces it (`_TEMPLATE.md`, which every plan is copied from) untouched, so the next plan would have reproduced it silently | review (caught it) — but the ratchet's own wording is what should have: "a fix without a check buys one bug" is aimed at missing checks, and does not say plainly that fixing the instance while the generator stays broken buys exactly one instance | none yet — candidate: when a defect is found in a file that was copied from a template or generator, the fix must name the origin and either fix it or log why it cannot be fixed here (template-owned, upstream, etc.). Three of today's entries are template-owned and would each recur in the next generated project |
| 2026-08-14 | one root cause (the plan parser rejecting a section banner) required four separate pull requests — the plan fix, the template fix, the ratchet entry, and the feature rebase behind them — because gated documents may not travel together, and the chain then blocked on itself when one link referenced another that had not landed | none existed — the separation rule is deliberate and correct, but nothing accounts for its cost: a single defect fans out into a serialized queue, and each additional link is another place for the queue to stall | none yet — candidate: state the ordering explicitly (ratchet entry, then governance documents that cite it, then the work), and allow a single pull request to carry an escapes entry together with the template or plan fix it describes when they share one root cause — the reason for separating them is that a change must not carry its own revision of the standard it is judged against, and an append-only incident log is not that standard |

| ESC-17 | 2026-08-14 | `template-sync` cannot pass for any `copier update` that conflicts: it replays the update from the base commit and demands a byte-identical tree, but a conflict is precisely the case copier hands back to a human, and the replay reproduces the same markers — so the only tree that satisfies the check is one that commits conflict markers to the default branch. Second occurrence, after the same cause blocked #4 | CI (`template-sync` — present, running, and blocking the very change it exists to authorise; it has no concept of a resolved conflict, only of an unexplained difference) | unverified — pending: the fix is upstream in the template and owner-owned. Candidate: keep the replay's PRE-resolution tree and require the pull request's tree to differ from it only inside hunks copier marked as conflicted, so a hand edit in an untouched file still fails byte-for-byte while the one edit copier explicitly delegates to a human is permitted. Merged meanwhile by owner bypass on #28 |
| ESC-18 | 2026-08-14 | the review gate blocked two of this session's own pull requests for defects this process already documents in writing: one asserted an incident was "logged in `docs/escapes.md`" while that entry sat unmerged on a sibling branch — the ordering rule's exact failure mode, recurring — and one added an id cell to sixteen already-landed rows of this append-only ledger, which is a rewrite however little the text changed. Both were written by an agent that had quoted both rules, in this session, in the pull request bodies that broke them; reciting a rule is not the same as having a mechanism | review (it caught both, correctly, and named the reasoning). Nothing mechanical covers either: `escape-refs.sh` resolves rigid `ESC-<n>` ids ONLY, and only inside `AGENTS.md`, `docs/DESIGN.md` and `docs/plans/`, so a prose claim about the ledger in `docs/BACKLOG.md` or `docs/JOURNAL.md` is out of scope on both axes; and nothing at all checks that this file's existing rows are untouched | unverified — pending, and both candidates are gate paths so the fix is the owner's. (1) An append-only check on `docs/escapes.md`: fail the pull request when its diff modifies or removes any table row that exists at the base commit, permitting only additions. Mechanically simple, no prose parsing, and it would have caught the second failure exactly. (2) Widen `escape-refs.sh`'s document set from the three gated documents to every markdown file under `docs/`, keeping rigid-id matching — that catches an id citation anywhere, though NOT a prose claim naming the file, which the template deliberately declined to parse and which is what actually happened here. Neither has been run; the first is the one worth building |
| ESC-19 | 2026-08-14 | an agent knowingly departed from a documented rule — it added an id cell to sixteen rows of this append-only ledger — and recorded its justification in the pull request body instead of raising the objection with the owner first. The review gate blocked it, correctly, and the cost was a rewritten branch and a full re-run of every check. Both escalation paths existed and neither was used: say it in chat, or write the objection into `docs/BACKLOG.md` as a proposal. A pull request body is the one place an objection cannot be answered before it has already cost something | review (it caught this, correctly, and named the rule). Nothing catches it EARLIER, which is the actual gap — the review gate is the owner's last line of defence, and it was being used as the agent's first | unverified — pending. Candidate: an agent runs the review gate against its own diff BEFORE opening the pull request, exactly as the mechanical gate scripts were run locally before #28, so a foreseeable BLOCK surfaces in the worktree instead of in the owner's queue. Unconfirmed whether `CLAUDE_CODE_OAUTH_TOKEN` is reachable from a working environment; if it is not, the always-available fallback is a written pre-flight — name each rule the diff touches, state whether it complies, and stop rather than argue when it does not |
| ESC-20 | 2026-08-14 | four pull requests were left open at once while the first of them was red, and two of those cited a ledger entry that sat unmerged inside it. The two that passed auto-merged ahead of the one they depended on, so the blocked branch then needed `main` merged in and every check re-run — by hand, by the owner. The ordering rule that prevents exactly this (the ratchet entry lands, then the document citing it, then the work) was quoted in the same pull request bodies that broke it | review (it caught each dangling citation, one pull request at a time). Nothing sees the QUEUE: every check judges a single pull request in isolation, so "three open at once, two of them depending on the third" is invisible to all of them, and the cost lands on the owner rather than on any gate | unverified — pending, and any mechanical form is a gate path so it is the owner's. Candidate: one open pull request per agent at a time — land it before opening the next, and where an ordering exists, obey it rather than opening the chain in parallel. Mechanically: fail a pull request while another from the same author is open and not green. The behavioural form needs no new gate and can start immediately; the mechanical form is what makes it survive an agent that forgets |

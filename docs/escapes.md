# Escapes — grimsverk-template

Append-only log of defects that reached a **generated project** and originated
**here**, in the template. One line per escape, newest at the bottom. Never
rewrite an entry; if something turns out to be wrong, append a correction.

This is the template side of the ratchet in the generated `AGENTS.md`: nothing
that escapes gets fixed without also adding a permanent check that would have
caught it. A generated project logs an escape as *its* incident record, and that
is the right place for it — but the fix and the check often live here, in the
file every future project is copied from, and a log entry in one project cannot
explain a check in another repository. So this file exists to keep the reasoning
next to the check.

It is deliberately **project-agnostic**. Entries name the defect and the check,
never the project's domain, its owner, or its business. If a line only makes
sense to someone who knows the originating project, it is written wrong.

**The evidence lives in `docs/projects/<repo>/`.** Each originating project's
records are mirrored there verbatim, under a directory named after the
repository, so that with many projects it stays visible *which* project produced
which evidence. An entry here records the *check*; it cites the mirror rather
than re-narrating the incident. Nothing is deleted from the originating project
— its log is its own incident record and stays append-only there.

**The "check added" column has a rule.** Record either a check that has actually
been run — red against the defect, green against the fix — or a proposal
explicitly marked *unverified*. An untested suggestion in that column reads with
exactly the same authority as a demonstrated one, and sends the next reader down
a dead end. Say which environment a check was verified in when that matters: a
check that needs an engine subscription is not verified by a runner that has no
subscription.

**Every entry has an id** — `ESC-<n>`, the next unused integer — and the id is
how commits and documents refer to it. An entry may land in two steps: a
one-line **stub** first (id, date, what escaped, gate column, and a check column
of *unverified — pending: <the check you intend>*), then, once the fix and its
check have merged, an appended **correction row with the same id** recording the
demonstrated check. Entries are records, never authorization — nothing passes
any gate because of anything in this file.

| Id | Date | What escaped | Which gate should have caught it | Check added |
| --- | --- | --- | --- | --- |
| _ESC-<n>_ | _YYYY-MM-DD_ | _what went wrong, one clause, no project specifics_ | _template CI / render / gate fixtures / none existed_ | _what now exists so it can't recur, or "unverified — pending: <planned check>"_ |

<!-- Append below, newest at the bottom. Never rewrite an entry; if one turns
out to be wrong, append a correction. -->

| ESC-1 | 2026-08-14 | `spawn-worker.sh` launched `codex exec --full-auto`, an argument codex-cli removed (0.147.0 rejects it), so every codex worker died at launch before reading its prompt | none existed — nothing ran the orchestration path, in CI or anywhere else, so a breaking change in an external CLI surfaced only when a human first needed the tool | `--approve-for-me` replaces it, and `tests/test-spawn-worker.sh` pins the flag through the new `--print-command` (verified: red on the old script, green on the new). That the real CLI *accepts* it is provable only by `tests/smoke-worker.sh`, which is **unverified for codex** — codex was not installed on the machine this was written on |
| ESC-2 | 2026-08-14 | worker worktrees were created under `.claude/worktrees/`, which Claude Code treats as protected, so a sandboxed headless worker was refused every write and — having nobody to prompt — produced an empty branch and exited 0 | none existed — same gap: the orchestration path was first executed at the moment it was first needed | worktrees moved to `.worktrees/` (gitignored, dot-prefixed so pytest and ruff skip it). `tests/test-spawn-worker.sh` asserts the location and that nothing is written under `.claude/`; verified red against the old script and green against the new, and confirmed live by `tests/smoke-worker.sh` with the `claude` engine. `--bypass-sandbox` was rejected as the fix: it drops the sandbox for unreviewed model-written code to work around a directory name |
| ESC-3 | 2026-08-14 | a headless worker that did nothing exited 0, so the spawn script reported success for a run that wrote no file and made no commit; the only thing between that and a silently empty pull request was the orchestrator remembering to diff each branch | none existed — "never let an empty branch pass as a success" was prose in `.claude/commands/orchestrate.md`, followed by the agent and checked by nothing | `spawn-worker.sh` compares the branch against the commit it started from and exits 3 with `commits=0` when nothing landed, distinguishing "wrote nothing" from "wrote and never committed". Fixtures cover both, plus an engine failure and a base branch that moves mid-run; verified red/green. It earned its keep immediately — it is what caught the entry below, on its first live run |
| ESC-4 | 2026-08-14 | the engine was checked with `command -v` and nothing else, so an engine that is installed but cannot authenticate failed deep inside a headless run; on an account with no codex subscription the DEFAULT path could not work at all, and it presented as an argument error, sending the first diagnosis at the wrong problem | none existed — presence on `PATH` was treated as usability, which it never was | a preflight probe per engine before any worktree is created, failing with "engine X is installed but not usable" and printing the probe, its exit code and its output. `claude auth status` is verified live; the codex probe (`codex login status`) is **unverified** — no codex on the machine. Fixtures cover a failing probe, a probe that reports a signed-out account while exiting 0, and `--skip-preflight` |
| ESC-5 | 2026-08-14 | a `claude` worker could not write anything at all: a fresh worktree is a workspace nobody has trusted interactively, so the project's own `.claude/settings.json` allow list is ignored there, and the default permission mode asks for approval a headless run has nobody to give — the worker answered "I need your permission to write that file" and exited 0 | none existed — but note *how this was found*: the empty-branch check above caught it on the first live smoke run, which is the ratchet working as designed rather than another escape to the owner | the `claude` engine now passes `--permission-mode acceptEdits` plus an explicit `--allowed-tools` whitelist (git add/commit/status/diff/log/show and the language test runners) on the command line, where workspace trust does not apply. A whitelist rather than deny rules: `git push` and `--no-verify` are not forbidden, they are unreachable. Verified live end-to-end by `tests/smoke-worker.sh claude`, and the flags are pinned by fixtures — including that the variadic `--allowed-tools` does not swallow the prompt |
| ESC-6 | 2026-08-14 | `plan-parse.sh` treated every heading matching `^#+[[:space:]]*Slice` as a slice, so the shipped `_TEMPLATE.md`'s `## Slices` section banner parsed as a slice declaring no files and no estimate — rejecting every plan copied from the skeleton, with a symptom two pull requests downstream of its cause: the plan check failed, and the review gate, handed an empty facts table it is told to trust, blocked on that alone | none existed — nothing parsed the skeleton every plan is copied from, and nothing parsed a plan except the single one a pull request happened to resolve to | three parts: the pattern requires whitespace after the word; the banner is renamed to `## The slices`; and `.github/scripts/plan-lint.sh` parses every real plan in the tree on every pull request, so a malformed plan fails where the error can name it. Fixtures cover the plural banner, a banner-only plan, a slice with no estimate and one with no file list (verified red/green). Deliberately **not** a check that parses `_TEMPLATE.md` — its placeholders are unparseable by design, so that check would have been red the day it landed; the shipped skeleton is checked instead for headings that look like a slice without being one |
| ESC-7 | 2026-08-14 | `coverage.sh` recognised `**R<digits>**` and silently skipped anything else, so an id like `R2a` was never counted as covered and never reported as missing — it read like a tracked requirement while being tracked by nothing, and a plan claiming it was rejected with no hint why | CI (the gate was present, and failed open on exactly the input it could not understand) | it now fails on any `**R…**`/`**S…**` in the design, or any `covers:` entry, that is id-shaped but malformed. The rule is anchored on a digit after the letter so bold prose — `**Rationale**`, `**Scope**` — is never a false positive; `tests/test-coverage.sh` fixtures cover both directions, plus a plan covering an undefined-but-well-formed id, which stays a report rather than an error (verified red/green). The design skeleton's own ids are asserted to pass the gate it ships with |
| ESC-8 | 2026-08-14 | the `chore/`/`docs/` planning exemption capped at 50 added lines with no path for the two documents the process itself demands — a completed design doc runs to hundreds of lines, the plan template is 124 before anything is filled in, and a plan branch cannot resolve to a plan that does not exist yet, which is the rule's whole point | plan (none existed: the gate handled planned code and template syncs, and the planning documents are neither). Downstream it forced three owner bypasses in one session, and a gate bypassed three times in a day is one nobody will trust on the day it matters | `plan-resolve.sh` exempts from the size cap a branch whose diff is confined to `docs/plans/` or `docs/DESIGN.md`. It is a whole-diff test, not per-file, so a plan cannot be cover for code smuggled alongside it. Fixtures: a 200-line plan branch passes, a 400-line design-doc branch passes, the same plan plus 120 lines of code is still capped, and an oversized `chore/` branch is still capped (verified red/green) |
| ESC-9 | 2026-08-14 | the two blind workers for a slice were given asymmetric briefs — the coder's said records are sorted canonically, the test author's said only that they round-trip — so the two built to different contracts and the suite failed at assembly on a behaviour the plan never stated, which reads exactly like a bug and is not one | none existed — the plan's Signatures block is the declared contract between two agents who cannot see each other, but anything the orchestrator adds beyond it lives only in the prompts, and nothing compares the two prompts | `.claude/commands/orchestrate.md` now requires ONE shared contract block, quoted verbatim into both briefs, and states that any behaviour both sides can observe belongs in the plan rather than in a prompt. **Presence check only** (`tests/test-render.sh`, verified red against the pre-fix document and green after): it catches the rule being dropped by a later template edit, and cannot catch an agent ignoring it — no check can read a prompt that was never written down. The retry that used a shared block by hand did make the disagreements vanish, which is evidence for the rule, not a test of it |
| ESC-10 | 2026-08-14 | an agent waited on "the pull request is no longer open", a condition a red pull request never satisfies, so a failure was indistinguishable from work still in progress — and where nothing was watching at all, indistinguishable from success: a red pull request sat unnoticed until the owner mentioned it | none existed — the handoff to the merge pipeline was treated as the finish line, and nothing said the agent must keep listening for the pipeline's answer | `orchestrate.md` and `deliver.md` both state the exit condition: wait until **no check is still pending**, never until the pull request is no longer open, and report which checks failed rather than that the wait ended. Presence check in `tests/test-render.sh` (verified red/green). Note this constrains the still-undecided waiting *strategy* in `/deliver` step 4 rather than resolving it — that remains an owner ruling |
| ESC-11 | 2026-08-14 | a governance document asserted "logged in `docs/escapes.md`" while that entry sat unmerged in another pull request, so the claim was false at the base commit the review gate reads — it blocked the same pull request twice, the second time purely because of ordering | review (it caught both, correctly) — but nothing stated the ordering rule that would have avoided it: `AGENTS.md` required each gated document to travel alone and was silent on what happens when one cites another | `AGENTS.md` now states the order: the ratchet entry lands first, then the document that cites it, then the work. Presence check in `tests/test-render.sh` (verified red/green). The stronger check is now built as well: entries carry rigid ids (`ESC-<n>`), and `.github/scripts/escape-refs.sh` resolves every id citation in the gated documents against `docs/escapes.md` at the base commit, failing when one dangles (fixtures in `tests/test-escape-refs.sh`, verified red/green). Rigid ids are what made it buildable — the prose-parsing version stayed unbuilt because a false positive on a gate that fails closed is worse than the rereading it saves |
| ESC-12 | 2026-08-14 | the ratchet accumulated a candidate check that could never work — parsing `_TEMPLATE.md` in CI, whose placeholders the parser rejects by design — so it would have been red the day it was added and switched off soon after; it was caught by chance, not by any gate | none existed — a candidate check is prose in a table cell, and nothing required it to have been run, so an untested suggestion carried the same authority as a demonstrated one | `AGENTS.md` now requires the "check added" column to record either a demonstrated check (red against the defect, green against the fix, both observed) or an explicitly-marked *unverified* proposal. Presence check in `tests/test-render.sh` (verified red/green). This log follows the same rule, which is why two entries above say plainly that the codex path is unverified here |
| ESC-13 | 2026-08-14 | one root cause fanned into four serialized pull requests — the fix, the template fix, the ratchet entry, and the work behind them — because gated documents may not travel together, and the chain then blocked on itself when one link cited another that had not landed | none existed — the separation rule is deliberate and correct, but nothing accounted for its cost | the ordering rule above is the answer, and the proposed relaxation (one pull request carrying an escapes entry together with the fix it describes) was **considered and rejected**, with the reasoning recorded in `AGENTS.md` so it is not relitigated: the review gate reads this log as evidence, so a change appending its own exculpatory entry supplies the evidence it is judged by, and an agent logging its own escape is precisely where a self-serving entry would come from. Presence check in `tests/test-render.sh` (verified red/green). The serialization cost itself is answered without reopening that ruling: entries carry ids and may land first as a one-line **stub** (check column marked *unverified — pending*), completed by an appended correction row after the fix merges — so the entry-first ordering costs one trivial pull request instead of blocking a chain, and citations are by id, backward-only, enforced by `escape-refs.sh`. No gated document ever travels with work |
| ESC-14 | 2026-08-15 | `template-sync` cannot pass for any `copier update` that conflicts. It replays the update from the base commit and demands a byte-identical tree, but a conflict is exactly the case copier hands back to a human, and the replay reproduces the same markers — so the only tree that satisfies the check is one committing conflict markers to the default branch. Second occurrence in one project, on separate updates | template CI (`template-sync` — present, running, and blocking the very change it exists to authorise; it has no concept of a resolved conflict, only of an unexplained difference) | unverified — pending: keep the replay's PRE-resolution tree and require the project's tree to differ from it only inside hunks copier marked conflicted, so a hand edit in an untouched file still fails byte-for-byte while the one edit copier explicitly delegates to a human is permitted. Not built here; recorded so the next update does not rediscover it. Evidence: `docs/projects/find_best_mobo/escapes.md`, ESC-17 |
| ESC-15 | 2026-08-15 | nothing checks that an append-only ledger's existing rows are untouched. An agent retrofitted an id cell onto sixteen already-landed rows of `docs/escapes.md` — a rewrite however little the text changed — and only the review gate caught it. `escape-refs.sh` resolves rigid ids and says nothing about whether prior rows still exist | review (it caught this, correctly). Nothing mechanical covers it: the ledgers are declared append-only in `AGENTS.md` and enforced by nobody | the append-only mechanism now exists for the oracle's design ledger — `.github/scripts/oracle-decisions.sh` fails when a decision present at the base commit is modified or removed, with fixtures in `tests/test-oracle-decisions.sh` (verified red against a modified decision and a deleted one, green against a pure append). It is **not yet applied to `docs/escapes.md`**, which is the same check over a different file and the obvious next step; recorded rather than claimed |
| ESC-16 | 2026-08-15 | an agent knowingly departed from a documented rule and recorded its justification in the pull request body instead of raising the objection first. The review gate blocked it, correctly, and the cost was a rewritten branch and a full re-run of every check. Both escalation paths existed in `AGENTS.md` and neither was used: say it in chat, or write the objection into `docs/BACKLOG.md` as a proposal. A pull request body is the one place an objection cannot be answered before it has already cost something | review (it caught this, correctly, and named the rule). Nothing catches it EARLIER, which is the actual gap — the review gate is the owner's last line of defence and was being used as the agent's first | unverified — pending: an agent runs the review gate against its own diff BEFORE opening the pull request, so a foreseeable BLOCK surfaces in the worktree instead of in the owner's queue. Whether the review engine's credential is reachable from a working environment is unconfirmed; the always-available fallback is a written pre-flight — name each rule the diff touches, state whether it complies, and stop rather than argue when it does not. Evidence: `docs/projects/find_best_mobo/escapes.md`, ESC-19 |
| ESC-17 | 2026-08-15 | no gate sees the QUEUE. Four pull requests were left open at once while the first was red, and two cited a ledger entry sitting unmerged inside it; the two that passed merged ahead of the one they depended on, so the blocked branch needed the default branch merged in and every check re-run by hand, by the owner. The ordering rule that prevents exactly this was quoted in the same pull request bodies that broke it | review (it caught each dangling citation, one pull request at a time). Every check judges a single pull request in isolation, so "four open at once, two depending on a third" is invisible to all of them and the cost lands on the owner | unverified — pending: one open pull request per agent at a time — land it before opening the next, and where an ordering exists obey it rather than opening the chain in parallel. Mechanically, fail a pull request while another from the same author is open and not green. The behavioural form needs no new gate; the mechanical form is what makes it survive an agent that forgets. Evidence: `docs/projects/find_best_mobo/escapes.md`, ESC-20 |

# {{ project_name }} — agent guidelines

Canonical working instructions for any coding agent in this repository.
Tool-specific entry points (e.g. `CLAUDE.md`) only point here; rules are
never duplicated elsewhere.

> **Status:** structured placeholder — rules are populated in the
> rule-extraction pass.

## Workflow

TODO (populated in rule-extraction pass)

## Enforcement

TODO (populated in rule-extraction pass)

<!-- Enforcement is implemented in git hooks (.pre-commit-config.yaml) and CI
     (.github/workflows/ci.yml). This section describes those gates; it does
     not replace them. -->

## Language-specific

TODO (populated in rule-extraction pass)
